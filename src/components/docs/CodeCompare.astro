---
import { Code, Tabs, TabItem } from '@astrojs/starlight/components';
import SandpackDemo from './SandpackDemo';
import { exampleSlugs, getExample, type ExampleSlug } from '../../data/examples';

interface Props {
  title: string;
  slug: string;
  threeCode: string;
  r3fCode: string;
}

const { title, slug, threeCode, r3fCode } = Astro.props;
const example = exampleSlugs.includes(slug as ExampleSlug)
  ? getExample(slug as ExampleSlug)
  : undefined;
---

<section class="code-compare">
  <header class="section-header">
    <h2>{title}</h2>
    <p>Switch between the React Three Fiber and Three.js examples, then test the R3F result in the live preview below.</p>
  </header>

  <Tabs syncKey={`${slug}-runtime`}>
    <TabItem label="React Three Fiber">
      <Code
        code={r3fCode}
        lang="jsx"
        meta='title="React Three Fiber"'
      />
    </TabItem>

    <TabItem label="Three.js">
      <Code
        code={threeCode}
        lang="js"
        meta='title="Three.js"'
      />
    </TabItem>
  </Tabs>

  <SandpackDemo
    slug={slug}
    title={title}
    r3fCode={r3fCode}
    versions={example?.meta.versions ?? {}}
    client:visible
  />
</section>

<style>
  .code-compare {
    margin: 2rem 0;
  }

  .section-header {
    margin-bottom: 1rem;
  }

  .section-header h2 {
    margin: 0;
  }

  .section-header p {
    margin: 0.5rem 0 0;
    color: var(--sl-color-gray-3);
  }
</style>
